# Description: Solve a quadratic equation(ax**2 + bx + c = 0)

# Inputs: 
#   #input.a vplib.math (ax**2)
#   #input.b vplib.math (bx)
#   #input.c vplib.math (c)

# Output:
#   #sol1 vplib.math
#   #sol2 vplib.math

function #vplib:math/api/quadratic/solve